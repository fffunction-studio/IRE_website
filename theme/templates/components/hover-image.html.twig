
  <figure class="hover-figure {{ figure_class|join(' ') }}">
    <img class="default-image {{ image_class|join(' ') }}" {{ image|get_timber_image_responsive(size) }}>
    <img class="hover-image {{ hover_image_class|join(' ') }}" {{ hover_image|get_timber_image_responsive(size) }}>
  </figure>

